@model GeekHunters.GRS.Web.ViewModels.Home.HomeIndexModel

@{
    Model.Title = "Geek Hunters";

    Layout = "~/Views/Shared/_Layout.cshtml";
}

<ul id="menu" class="menu">
    <li data-url="@Url.Action("GetCandidatesView","Home")">Candidates</li>
    <li data-url="@Url.Action("GetSkillsView","Home")">Skills</li>
</ul>
<button class="k-button btn-registercandidate">Register Candidate</button>

@*<div class="container" style="background-color:green;">
    <nav class="navbar navbar-default navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand" href="@Url.Action("Index", "Home")">@Model.ApplicationName</a>
            </div>

            <div class="collapse navbar-collapse">
                <ul class="nav navbar-nav navbar-left">
                    <li><a href="#" onclick="LoadCandidate();">Candidates</a></li>
                    <li><a href="#" onclick="LoadSkills();">Skills</a></li>
                </ul>

                <ul class="nav navbar-nav navbar-right">
                    <li><button type="submit" class="btn btn-default navbar-btn" data-toggle="modal" data-target=".modal-addcandidate">Register Candidate</button></li>
                </ul>
            </div>
        </div>
    </nav>
</div>*@

<div class="main-content">
@{
    Html.RenderAction("GetCandidatesView", "Home");
}
</div>

@{
    Html.RenderAction("GetGeekRegistrationView", "Home");
}